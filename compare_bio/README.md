Small helper for validating the parsers by comparing their output with Rust-Bio
for any file. The extension of input files is autorecognized.

Usage:
```
cargo run --release file.fasta
cargo run --release file.fastq
```
